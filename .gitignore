# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build outputs
src-tauri/target
src-tauri/gen/schemas

# Native bundles (kept out even if produced outside target/)
*.app
*.dmg
*.exe
*.msi
*.AppImage
*.deb
*.rpm

# PrintVault runtime data (settings and thumbnail cache live in the OS
# app-config / app-cache directories; these root-anchored patterns are
# defensive only and must not match src/thumbs/)
/printvault-settings.json
/settings.json.tmp
/thumbs/
*.thumb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
